<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Nabbit.Views.CheckoutPage">
    <ContentPage.Content>
        <StackLayout Margin="{StaticResource PageMargin}">
			<DatePicker x:Name="datePicker"
						MinimumDate="{Binding MinDate}"
						Date="{Binding PickupDate}"/>
			<TimePicker x:Name="timePicker"
						Time="{Binding PickupTime}" />
			
			<Label Text="{Binding Order.OrderTotal, StringFormat='{0:c}'}" FontSize="Large" FontAttributes="Bold" />
			<Entry Text="{Binding CreditCardNumber}" Placeholder="Enter Your credit card number" />
			<StackLayout Orientation="Horizontal">
				<Entry Text="{Binding ExpiryDate}" Placeholder="Expiry date" />
				<Entry Text="{Binding CVV}" Placeholder="CVV" />
			</StackLayout>
			<Button Text="Confirm Payment" VerticalOptions="End" Clicked="PurchaseClicked" />
		</StackLayout>
    </ContentPage.Content>
</ContentPage>
	