<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Nabbit.Views.ThankYouPage">
    <ContentPage.Content>
        <StackLayout Margin="{StaticResource PageMargin}" VerticalOptions="CenterAndExpand">
			<Frame CornerRadius="180"
                   BorderColor="{StaticResource secondaryColor}"
                   HasShadow="true"
                   Scale="0.7"
                   BackgroundColor="Black">
                <Image Source="nabbit_logo_transparent_outline"/>
            </Frame>
            
           
            <Label x:Name="signInLabel" 
                   HorizontalOptions="CenterAndExpand"
                   Style="{StaticResource primaryDarkLabel}"
                   FontSize="Medium"
                   Text="Thank you for your order!" />
			<Button Text="Return to Menu" Clicked="ReturnClicked"
                    BorderRadius="0" FontSize="Large"
                    TextColor="{StaticResource primaryTextColor}"
                    BackgroundColor="{StaticResource primaryColor}"
                    HorizontalOptions="FillAndExpand" />
		</StackLayout>
    </ContentPage.Content>
</ContentPage>